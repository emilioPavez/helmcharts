apiVersion: v1
kind: Service
metadata:
  name: {{ include "fl.fullname" . }}
  labels:
    {{- include "fl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fl.selectorLabels" . | nindent 4 }}
